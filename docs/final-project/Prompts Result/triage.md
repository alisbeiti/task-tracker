# Triage of AI review comments (review.md)

Using the manual note that item 1's resolution should be "drop the no-Docker/no-CI language from CLAUDE.md" as the confirmed direction for that item. All buckets below are based on evidence gathered directly from the repo (file reads + greps), not fresh guesses.

| Comment summary | Bucket | Evidence needed or evidence found | Action |
|---|---|---|---|
| CLAUDE.md forbids Docker/CI while this diff adds both | **Useful** | Evidence found: `CLAUDE.md:5,101` read directly ("no Docker," "Do not add... Docker, cloud hosting, CI/CD"); `Dockerfile` and `.github/workflows/ci.yml` confirmed new in same diff; `docs/mid-course/mini-adr.md` read in full — never mentions Docker/CI, so the citation doesn't hold either. Manual note confirms the resolution direction. | Edit `CLAUDE.md` to remove/replace the "no Docker, no CI/CD" language (lines 5 and 101) since Docker/CI now exist in the branch. |
| `storage.add_comment`'s blank-text `ValueError` would 500 instead of 422 if reached | **Noise** | Evidence found: grep confirms `add_comment` has exactly one caller (`app/main.py:add_task_comment`), and that call path is already guarded upstream by `CommentCreate.validate_text`, so the branch is unreachable today. Real observation, but the failure mode never actually occurs with any code path currently in the repo — it's a hypothetical about future misuse, not a live bug. | No action. |
| ~70 lines of unused JSON load/save/hydrate functions in `storage.py` | **Useful** | Evidence found: repo-wide grep for `load_tasks_from_json`/`save_tasks_to_json` matches only their own definitions — no caller anywhere. Also conflicts with `CLAUDE.md:100`'s explicit in-memory-only rule. | Remove `_coerce_comments`, `_hydrate_task`, `load_tasks_from_json`, `save_tasks_to_json` (`app/storage.py`), or confirm with the module owner why they should stay before the branch ships. |
| CI never builds/runs the new Docker image | **Noise** | Evidence found: `ci.yml` steps read in full — checkout, Python setup, `pip install`, `pytest -v` only, no `docker build`/`docker run`. True, but this is a nice-to-have for a project that (per README/CLAUDE.md) explicitly disclaims production readiness, and it's entangled with item 1 (Docker's in-scope status is itself unresolved). | No action. |
| `temp_verify_comments.py` is a throwaway script committed at repo root | **Useful** | Evidence found: file read in full — uses `print()`, not assertions; `.dockerignore:43` excludes it; confirmed absent from README.md and CLAUDE.md via full reads of both. | Delete the file, or move it into `tests/` and convert to real assertions if it has ongoing value. |
| README's "status transitions are restricted" claim doesn't scope it to PATCH-only | **Useful** | Evidence found: `app/main.py` read directly — `create_task` never calls `validate_status_transition`; `update_task` only calls it `if payload.status is not None`. So `POST /tasks` can set any status with no check, which the current wording doesn't disclose. | Reword `README.md:7` and `:203` to say the check applies "on update via PATCH," not as a blanket statement. |
| No test coverage for `Done→ToDo`, `InProgress→ToDo`, `Done→InProgress` transitions | **Useful** | Evidence found: `tests/test_status_transition.py` read in full — only covers `ToDo→InProgress` (valid), `ToDo→Done` (invalid), and same-status no-op. `tests/test_tasks.py` diff confirmed no additional transition cases beyond the same-status rename. | Add 2-3 test cases in `tests/test_status_transition.py` covering the two untested rejections and the one untested valid reverse transition. |

**Summary:** 5 Useful, 2 Noise, 0 Wrong, 0 Needs manual check (all seven were verifiable against code already read in this session).
