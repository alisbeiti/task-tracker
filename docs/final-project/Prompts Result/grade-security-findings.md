# Security Finding Grades

| Finding ID | Proposed grade | Reason | Evidence needed or evidence used | Student decision to confirm |
|---|---|---|---|---|
| SEC-01 | Valid | This is a real production-boundary risk, but not a defect in the stated local-learning scope. Any browser origin can call the API, including mutation endpoints; because there is no auth, CORS does not protect the task data or writes once the service is exposed beyond localhost. Medium is reasonable as a conditional deployment risk. | `app/main.py:43-49` permits all origins, methods, and headers. CRUD routes have no authorization. `docs/final-project/agents.md:97-105` and `CLAUDE.md:97-103` explicitly make no-auth an intentional course decision. | Confirm the project is intended to remain local-only. If so, retain as a documented “before deployment” limitation rather than a current-course bug. |
| SEC-02 | Valid | The lack of server-side limits is real: a caller can bypass frontend limits, submit very large descriptions/comments/assignees, create unlimited in-memory tasks/comments, and retrieve all data in one response. It matters outside the learning context. Medium is plausible but context-dependent; the current local app’s exposure is limited. | `app/models.py:22-28` caps titles only; `TaskCreate` fields at `:31-38` do not constrain description or assignee. `CommentCreate` in `app/main.py:12-35` rejects blanks but has no length cap. `app/storage.py:9` retains all data in memory; `:67-93` returns the unpaginated task collection. | Confirm whether resource limits and pagination are outside the course requirements. If yes, keep as a production-readiness limitation, not a required course fix. |
| SEC-03 | Noise | The evidence describes broadly desirable supply-chain hardening, not a demonstrated security issue in this repo. Dependencies are directly version-pinned, and no vulnerable package/version was established. Lack of hash pins, digest pins, or a scanner is too generic on its own to be a useful security finding for this learning project. | `requirements.txt:1-7` has exact pins; `Dockerfile:2` uses a mutable base-image tag; CI installs directly from requirements. No advisory or compromised dependency was identified. | Confirm whether grading expects general production-hardening observations. Otherwise, mark as noise rather than an actionable security finding. |
