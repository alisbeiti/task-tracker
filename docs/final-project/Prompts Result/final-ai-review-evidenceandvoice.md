## AI-Assisted Coding - Module 5 Prompt Library

| Requirement | Present / Missing | Evidence from my draft | Minimal edit |
|---|---|---|---|
| One page or close to one page | Present | Whole doc is ~30 lines across 6 short sections. | None needed. |
| Own voice, not generic AI-policy language | Partial | Section 4 ("I read every AI-generated change before accepting it, make sure the code matches the task without adding extra work, and run the relevant tests") reads like standard AI-usage-policy phrasing rather than something tied to how you personally work. | Swap the general phrasing for the specific habit/moment it came from — needs course evidence to know what to substitute. |
| Each rule backed by a specific course incident or observation | Missing | Sections 1–5 state rules and preferences only ("I use AI when I need help breaking tasks into smaller steps...") — no reference to a specific bug, PR, file, command, or session from this project anywhere in the doc. | Needs course evidence — attach one concrete incident/observation per section (e.g., a specific case where you did or didn't reach for AI). |
| Mentions practical review habits, not vague aspirations | Partial | Section 4 names "run the relevant tests" — practical in kind, but no specific command, tool, or example of what it caught. Section 5's aspirational content is fine since that section is meant to be forward-looking. | Needs course evidence — name the actual command/tool (e.g., what you actually type or check) instead of "the relevant tests." |
| Decision Card is fully filled in | Present | All six blanks in Section 6 are filled: feature/review/debugging/infrastructure, never-paste, and one rule. | None needed. |
| Never-paste rule names a concrete type of data | Present | "I will never paste passwords, keys, or private data into an AI tool." — passwords and keys are concrete; "private data" is broader but doesn't weaken the concrete items already named. | Optional: could tie "private data" to something specific in this repo (e.g., `.env` values) — only if you have a real instance to point to. |
| 30-day re-read commitment is present | Missing | No mention of a re-read date, cadence, or review commitment anywhere in the draft. | Needs course evidence/decision — add one line stating when you'll re-read this (e.g., a specific date). |

One correctness note, not a style suggestion: Section 2 currently reads "I don't not use AI when the work includes private or sensitive information" — the double negative says the opposite of what Section 3/6 state elsewhere in your own draft. Worth a one-word fix ("don't not" → "don't") since it currently contradicts your non-negotiables.
